{
  "functions": {
    "api/**/*.js": { "runtime": "nodejs18.x" }
  },
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9"
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && pip install yt-dlp"
      }
    }
  ]
}